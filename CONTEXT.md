# EVA Tick

EVA Tick 为模型调用者提供可发现、可组合的市场及相关数据能力，并将长期产品契约与具体数据来源分离。

## Development Workflow and Security

- Codex 完成任何代码修改后，必须在结束当前任务前创建 Git commit，不得只将 Codex 产生的代码修改留在工作区。
- commit 必须聚焦于当前任务；只 stage 当前任务的文件，不得带入用户原有或其他任务的未提交修改。
- 每次 commit 前必须检查 `git status` 和 staged diff，确认提交范围与内容。
- 严禁提交任何秘钥或敏感信息，包括 API key、access token、密码、私钥、凭据以及包含真实值的环境配置文件。
- 敏感值必须通过环境变量或安全的密钥管理方式提供；示例文件只能使用明确的占位符。
- 如果发现敏感信息，必须先将其从提交中移除；若凭据可能已泄露，必须告知用户需要轮换凭据。

## Language

**稳定命令（Stable Command）**:
由 EVA Tick 拥有并长期保留其路径的市场数据能力；参数和返回字段可以随明确发布的版本演进。
_Avoid_: AKShare 命令、固定函数

**已发布命令路径（Published Command Path）**:
已经随正式版本交付、生成器后续不得自行改名或删除的 CLI 地址；升级时可以通过明确的人工作业决定删除或重新映射，路径保护也不表示参数和返回字段永久不变。
_Avoid_: 永久 API、固定 Schema

**稳定领域（Stable Domain）**:
由 EVA Tick 长期维护统一语言和兼容性边界的数据类别；首版包括股票、指数、基金、期货、期权、债券、外汇、宏观和交易日历。
_Avoid_: AKShare 模块、提供方领域

**上游命令（Upstream Command）**:
忠实暴露特定数据提供方能力、并明确继承其版本兼容风险的命令。
_Avoid_: 原始命令、稳定命令

**数据提供方（Data Provider）**:
为稳定命令或上游命令提供数据的外部系统或库；AKShare 是首个数据提供方，但不是 EVA Tick 的产品边界。
_Avoid_: 后端、AKShare（用于泛指提供方时）

**交易日历（Trading Calendar）**:
描述特定市场或交易所在一段日期内何时开放交易的日历；它跨越股票、期货和期权等资产类别。
_Avoid_: 股票日历、工作日历

**行情柱（Bar）**:
某一标的在固定时间周期内聚合的开盘价、最高价、最低价、收盘价及适用的成交数据。
_Avoid_: History、K 线、蜡烛图

**金融标的（Instrument）**:
可被唯一识别并引用的股票、指数、基金、合约、债券或货币对。
_Avoid_: Symbol、产品、品种（用于泛指所有标的时）

**标的目录（Instrument Catalog）**:
EVA Tick 维护的规范金融标的集合；同一标的只保留一个稳定身份，并记录数据提供方标识、别名、生命周期和可用能力。目录中的“全部”指至少有一个已启用数据提供方声明支持至少一种能力的标的，而不是全世界所有已发行标的。
_Avoid_: 股票列表、代码表、数据源标的列表

**标的标识（Instrument Identifier）**:
在明确标识体系及适用范围内引用金融标的的代码；交易代码通常还需要交易场所、标的类型或数据提供方范围才能唯一解释。
_Avoid_: Symbol（未说明范围时）、唯一代码（代码自身不唯一时）

**标的搜索（Instrument Search）**:
根据名称、标识和别名查找并排序真实目录候选的过程；搜索可以返回多个候选，不承诺确定唯一标的。
_Avoid_: 标的解析、代码生成

**标的解析（Instrument Resolution）**:
结合市场、交易场所、标的类型和所需能力等上下文，将用户输入及搜索候选确定为一个规范金融标的，或明确返回歧义及未找到状态。
_Avoid_: 模糊搜索、猜代码

**能力覆盖（Capability Coverage）**:
数据提供方在指定金融标的上可提供的能力及其状态、约束和观测时间；标的存在于目录中不表示所有数据能力均可用。
_Avoid_: 支持状态（未指明具体能力时）

**行情快照（Quote）**:
一个金融标的在某一观测时点的最新价格及适用的成交状态数据。
_Avoid_: 实时数据、盘口（不包含订单簿时）

**股票资料（Stock Profile）**:
描述股票及其发行公司的身份、分类和上市属性的参考信息。
_Avoid_: 基本面、公司信息

**财务数据（Financials）**:
发行公司的标准财务报表及从报表口径得出的关键指标。
_Avoid_: 基本面、财报数据（用于同时泛指报表和指标时）

**公司行为（Corporate Action）**:
由发行人发起、会影响证券持有人权益或证券属性的事件，例如分红、拆并股和配股。
_Avoid_: 公司事件、除权数据

**指数成分（Index Constituent）**:
在指定生效日期属于某一指数的金融标的；权重是该成员关系的可选属性，未知权重不等于零权重。
_Avoid_: 成分股（成分可能不是股票）、权重股

**市场服务（EVA 服务端）**:
位于 CLI 与数据提供方之间、通过版本化 HTTP 契约提供规范标的及市场数据的服务端；它负责目录持久化、标的解析、超时、重试、数据源回退和返回值规范化。
_Avoid_: AKShare 服务、CLI 后端（未说明职责边界时）

**数据提供方插件（Provider Plugin）**:
由 Cordis 管理生命周期、向市场服务注册标的和能力并实现数据读取契约的可插拔模块；一个市场可以同时启用多个插件。
_Avoid_: 数据源函数、固定适配器

**规范标的 ID（Canonical Instrument ID）**:
由市场、标的类型、身份所有者及其代码组成的服务端稳定标识，例如 `cn:equity:XSHE:000001`；数据提供方代码仅作为映射保存。
_Avoid_: 股票代码（规范 ID 不只包含交易代码）、AKShare symbol

**陈旧目录快照（Stale Catalog Snapshot）**:
数据提供方刷新暂时失败时，从持久化存储读取的最后一次成功标的目录；响应必须显式标记 `partial`、`stale` 和警告。
_Avoid_: 缓存命中（未说明新鲜度时）、当前目录
