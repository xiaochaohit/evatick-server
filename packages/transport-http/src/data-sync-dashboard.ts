import { adminNavigation, adminNavigationStyles } from './admin-navigation.js'

export const dataSyncDashboardHtml = String.raw`<!doctype html>
<html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>历史数据同步 · EVA</title><style>
@import url('https://fonts.googleapis.com/css2?family=DM+Mono:wght@400;500&family=Noto+Sans+SC:wght@400;500;600;700&display=swap');
:root{--ink:#e8f2ee;--muted:#839790;--paper:#07110f;--card:#0d1b17e8;--soft:#11231d;--line:#97c7b725;--green:#59d6ad;--mint:#2c96722e;--red:#ee806c;--shadow:0 24px 60px #0004}*{box-sizing:border-box}body{margin:0;min-height:100vh;color:var(--ink);font-family:"Noto Sans SC",sans-serif;background:radial-gradient(circle at 12% 2%,#2f8e6f29,transparent 32rem),var(--paper)}${adminNavigationStyles}
.shell{width:min(1240px,calc(100% - 40px));margin:auto;padding:40px 0 64px}.hero{display:flex;justify-content:space-between;align-items:flex-end;gap:24px;margin-bottom:20px}.eyebrow,.mono{font:500 11px "DM Mono",monospace}.eyebrow{color:var(--green);letter-spacing:.16em;margin-bottom:10px}h1{font-size:clamp(34px,5vw,54px);line-height:1;margin:0;letter-spacing:-.045em}h2{font-size:17px;margin:0 0 5px}.subtitle,.note{color:var(--muted);line-height:1.65}.subtitle{max-width:700px;margin:13px 0 0}.note{font-size:12px}.category-tabs,.tabs,.actions,.section-head,.run-head{display:flex;align-items:center;gap:8px}.category-tabs{padding:5px;border-radius:12px;background:#0a1713}.category-tab,.tab{border:0;background:transparent;color:var(--muted)}.category-tab{padding:10px 19px;border-radius:9px}.category-tab.active,.tab.active{background:var(--soft);color:var(--green)}button{border:0;border-radius:10px;padding:11px 13px;cursor:pointer;font:700 12px "Noto Sans SC",sans-serif;transition:.18s}button:hover{transform:translateY(-1px)}button:disabled{opacity:.4;cursor:not-allowed;transform:none}.primary{background:var(--green);color:#07130f}.secondary{background:var(--mint);color:var(--green)}.danger{background:#be493724;color:var(--red)}.small{padding:8px 10px}.panel{background:var(--card);border:1px solid var(--line);border-radius:20px;box-shadow:var(--shadow);overflow:hidden}.active-panel{padding:18px;margin-bottom:14px}.run{padding:16px;border-radius:15px;background:var(--soft);border:1px solid var(--line)}.run-head{justify-content:space-between}.badge{display:inline-flex;padding:5px 8px;border-radius:7px;font-size:11px;font-weight:700;background:var(--mint);color:var(--green)}.badge.error{background:#be493724;color:var(--red)}.progress-track{height:8px;border-radius:9px;background:#24362f;margin:14px 0 8px;overflow:hidden}.progress{height:100%;background:var(--green)}.progress-line{display:flex;justify-content:space-between;color:var(--muted);font-size:11px}.sync-panel{margin-bottom:14px}.toolbar{display:grid;grid-template-columns:minmax(220px,1fr) 110px 140px 140px auto auto;gap:9px;align-items:end;padding:15px;border-bottom:1px solid var(--line)}.field label{display:block;color:var(--muted);font-size:10px;margin-bottom:6px}.search{position:relative}.search span{position:absolute;left:12px;bottom:11px;color:var(--muted)}input,select{width:100%;height:40px;border:1px solid transparent;border-radius:9px;background:#13251f;color:var(--ink);padding:0 10px;font:500 11px "DM Mono",monospace;outline:none}input:focus,select:focus{border-color:var(--green)}.search input{padding-left:32px}.selection-bar{display:flex;justify-content:space-between;align-items:center;gap:12px;padding:10px 15px;background:#0a1713;color:var(--muted);font-size:11px}.table-wrap{overflow:auto}table{width:100%;border-collapse:collapse;font-size:11px}th{padding:11px 13px;text-align:left;color:var(--muted);font-weight:500;background:#0a1713;white-space:nowrap}td{padding:12px 13px;border-top:1px solid var(--line);vertical-align:middle;white-space:nowrap}tbody tr:hover{background:#10231c}input[type=checkbox]{width:auto;height:auto;accent-color:var(--green)}.identity{font-weight:600}.meta{color:var(--muted);font:500 10px "DM Mono",monospace;margin-top:3px}.row-action{padding:7px 9px;background:transparent;color:var(--green)}.empty{padding:28px;color:var(--muted);font-size:12px;text-align:center}.pager{display:flex;justify-content:space-between;align-items:center;gap:12px;padding:11px 15px;border-top:1px solid var(--line);color:var(--muted);font-size:11px}.tabs{padding:5px;background:#0a1713;border-bottom:1px solid var(--line)}.tab{padding:11px 15px}.tab-panel{display:none;padding:21px}.tab-panel.active{display:block}.section-head{justify-content:space-between;margin-bottom:14px}.history-row{display:grid;grid-template-columns:90px minmax(180px,1fr) 140px 150px auto;gap:12px;align-items:center;padding:13px 14px;background:var(--soft)}.history-row+.history-row{border-top:1px solid var(--line)}.details{grid-column:1/-1}.failure{display:grid;grid-template-columns:minmax(0,1fr) 1fr auto;gap:10px;padding:9px 0;border-top:1px solid var(--line);font-size:11px;color:var(--muted)}.error{min-height:18px;color:var(--red);font-size:11px}.schedule-form{display:grid;grid-template-columns:1.2fr .8fr .9fr .8fr .8fr auto;gap:9px;align-items:end;padding:15px;border:1px solid var(--line);border-radius:14px;background:var(--soft);margin-bottom:17px}.schedule-table .schedule-actions{display:flex;gap:6px}.schedule-hint{padding:11px 13px;border-left:3px solid var(--green);background:var(--soft);color:var(--muted);font-size:11px;margin-bottom:14px}
@media(max-width:980px){.toolbar{grid-template-columns:1fr 1fr 1fr}.toolbar .search{grid-column:1/-1}.schedule-form{grid-template-columns:1fr 1fr 1fr}.history-row{grid-template-columns:80px 1fr 1fr}.history-row>:nth-child(4){display:none}.hero{align-items:flex-start;flex-direction:column}}@media(max-width:600px){.shell{width:calc(100% - 24px);padding-top:24px}.toolbar,.schedule-form{grid-template-columns:1fr 1fr}.toolbar .search,.schedule-form .scope{grid-column:1/-1}.selection-bar,.pager,.section-head{align-items:flex-start;flex-direction:column}.history-row{grid-template-columns:1fr 1fr}.history-row>:nth-child(3){display:none}.failure{grid-template-columns:1fr}.tab-panel{padding:15px}.hide-mobile{display:none}}
</style></head><body>${adminNavigation('sync')}<main class="shell">
<header class="hero"><div><div class="eyebrow">HISTORICAL DATA SYNC</div><h1><span id="kind-title">股票</span>数据同步</h1><p class="subtitle">搜索并选择金融标的；加密货币按 Binance 与 Coinbase 交易场所分别同步。</p></div><div class="category-tabs"><button class="category-tab active" data-kind="equity">股票</button><button class="category-tab" data-kind="index">指数</button><button class="category-tab" data-kind="future">期货</button><button class="category-tab" data-kind="crypto">加密货币</button></div></header>
<section class="panel active-panel"><div id="active-run" class="run"><div class="empty">当前没有运行中的同步任务</div></div><div class="error" id="error"></div></section>
<section class="panel sync-panel"><div class="toolbar"><div class="field search"><label>搜索标的</label><span>⌕</span><input id="query" type="search" placeholder="代码、名称或 instrument ID" autocomplete="off"></div><div class="field"><label>行情周期</label><select id="interval"><option value="1m" selected>1 分钟</option><option value="1d">日线</option></select></div><div class="field"><label>开始日期</label><input id="start" type="date"></div><div class="field"><label>结束日期</label><input id="end" type="date"></div><button class="secondary" id="sync-selected" disabled>同步所选</button><button class="primary" id="sync-all">全部同步</button></div>
<div class="selection-bar"><span id="selection-count">已选 0 个股票</span><div class="actions"><span id="catalog-refreshed-at">目录尚未刷新 · 每天 08:00 自动刷新</span><button class="secondary small" id="refresh-catalog">刷新标的目录</button></div></div><div class="table-wrap" id="instrument-list"><div class="empty">正在读取股票目录…</div></div><div class="pager"><span id="instrument-summary">—</span><div class="actions"><button class="secondary small" id="previous" disabled>上一页</button><button class="secondary small" id="next" disabled>下一页</button></div></div></section>
<section class="panel"><div class="tabs"><button class="tab active" data-tab="history">运行历史</button><button class="tab" data-tab="schedules">定时任务</button></div>
<div class="tab-panel active" id="history"><div class="section-head"><div><h2>独立运行记录</h2><div class="note">手动与定时任务分开记录；失败重试直接更新原任务。</div></div><div class="actions"><button class="secondary small" id="refresh-runs">刷新</button><button class="danger small" id="clear-runs">清空记录</button></div></div><div id="run-list"><div class="empty">正在读取运行记录…</div></div></div>
<div class="tab-panel" id="schedules"><div class="section-head"><div><h2>创建定时任务</h2><div class="note">任务按北京时间执行；可选择当前分类全部标的或当前勾选项。</div></div><span class="note" id="schedule-count">—</span></div><div class="schedule-form"><div class="field scope"><label>同步范围</label><select id="schedule-scope"><option value="all">全部股票</option><option value="selected" disabled>当前勾选项（0）</option></select></div><div class="field"><label>周期</label><select id="schedule-interval"><option value="1d">日线</option><option value="1m">1 分钟</option></select></div><div class="field"><label>执行日期</label><select id="schedule-days"><option value="weekdays">周一至周五</option><option value="daily">每天</option></select></div><div class="field"><label>北京时间</label><input id="schedule-time" type="time" value="18:00"></div><div class="field"><label>回溯最近（天）</label><input id="schedule-lookback" type="number" min="1" max="90" value="10"></div><button class="primary" id="save-schedule">创建任务</button></div><div class="error" id="schedule-error"></div><div class="schedule-hint">“周一至周五”仅跳过周末。列表中的“下次执行”是计划触发时间；如果已有同步正在运行，任务会排队并在前一个任务结束后开始。</div><div class="table-wrap schedule-table" id="schedule-list"><div class="empty">正在读取定时任务…</div></div></div></section>
</main><script>
const byId=(id)=>document.getElementById(id),esc=(value)=>String(value).replace(/[&<>'"]/g,(char)=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]));
const now=new Date(),ago=new Date(now);ago.setUTCMonth(ago.getUTCMonth()-3);byId('end').value=now.toISOString().slice(0,10);byId('start').value=ago.toISOString().slice(0,10);
const requestedKind=new URLSearchParams(location.search).get('type');
const state={kind:['equity','index','future','crypto'].includes(requestedKind)?requestedKind:'equity',offset:0,limit:50,total:0,items:[],selected:{equity:new Set(),index:new Set(),future:new Set(),crypto:new Set()},activeRun:null,startSubmitting:false,searchTimer:null};
const kindName=(kind)=>kind==='equity'?'股票':kind==='index'?'指数':kind==='future'?'期货':kind==='crypto'?'加密货币':kind,time=(value)=>value?new Date(value).toLocaleString('zh-CN',{timeZone:'Asia/Shanghai',hour12:false}):'—';
const names={running:'运行中',completed:'已完成',completed_with_errors:'有失败项',cancelled:'已取消',failed:'运行失败'},recoveryNames={retrying:'正在重试',partially_recovered:'部分恢复',recovered:'重试后成功'},triggers={manual:'手动',scheduled:'自动',retry:'重试'};
async function request(url,options){const response=await fetch(url,options),body=response.status===204?null:await response.json();if(!response.ok)throw new Error(body?.detail||'请求失败');return body}
function selected(){return state.selected[state.kind]}
function updateSelection(){const count=selected().size,label=kindName(state.kind);byId('selection-count').textContent='已选 '+count+' 个'+label;byId('sync-selected').disabled=count===0||!!state.activeRun||state.startSubmitting;const option=byId('schedule-scope').options[1];option.textContent='当前勾选项（'+count+'）';option.disabled=count===0;if(!count&&byId('schedule-scope').value==='selected')byId('schedule-scope').value='all';byId('schedule-scope').options[0].textContent='全部'+label}
function renderInstruments(){const rows=state.items,label=kindName(state.kind);byId('instrument-list').innerHTML=rows.length?'<table><thead><tr><th><input id="select-page" type="checkbox" aria-label="选择本页"></th><th>标的</th><th>代码</th><th>交易场所</th><th class="hide-mobile">日线记录</th><th class="hide-mobile">分钟记录</th><th>分钟实际范围</th><th></th></tr></thead><tbody>'+rows.map((item)=>'<tr><td><input data-select="'+esc(item.instrument_id)+'" type="checkbox" '+(selected().has(item.instrument_id)?'checked':'')+' aria-label="选择 '+esc(item.name)+'"></td><td><div class="identity">'+esc(item.name)+'</div><div class="meta">'+esc(item.instrument_id)+'</div></td><td class="mono">'+esc(item.symbol)+'</td><td class="mono">'+esc(item.venue||'—')+'</td><td class="mono hide-mobile">'+item.daily_records.toLocaleString()+'</td><td class="mono hide-mobile">'+item.minute_records.toLocaleString()+'</td><td class="mono">'+esc(item.minute_first_trading_date||'—')+' → '+esc(item.minute_last_trading_date||'—')+'</td><td><button class="row-action" data-sync-one="'+esc(item.instrument_id)+'" data-name="'+esc(item.name)+' · '+esc(item.venue||'')+'">同步此标的</button></td></tr>').join('')+'</tbody></table>':'<div class="empty">没有找到匹配的'+label+'</div>';const pageBox=byId('select-page');if(pageBox){pageBox.checked=rows.length>0&&rows.every((item)=>selected().has(item.instrument_id));pageBox.indeterminate=rows.some((item)=>selected().has(item.instrument_id))&&!pageBox.checked}byId('instrument-summary').textContent='共 '+state.total+' 个'+label+' · 第 '+(state.total?Math.floor(state.offset/state.limit)+1:0)+' 页';byId('previous').disabled=state.offset===0;byId('next').disabled=state.offset+state.limit>=state.total;updateSelection()}
async function loadInstruments(){byId('instrument-list').innerHTML='<div class="empty">正在读取'+kindName(state.kind)+'目录…</div>';try{const query=new URLSearchParams({type:state.kind,q:byId('query').value,limit:String(state.limit),offset:String(state.offset)}),body=await request('/v1/data-sync/instruments?'+query);state.items=body.data;state.total=body.page.total;byId('catalog-refreshed-at').textContent=(body.meta?.catalog_refreshed_at?'目录更新于 '+time(body.meta.catalog_refreshed_at):'目录尚未刷新')+' · 每天 08:00 自动刷新';renderInstruments()}catch(error){byId('instrument-list').innerHTML='<div class="empty">'+esc(error.message)+'</div>'}}
function runCard(run){if(!run)return '<div class="empty">当前没有运行中的同步任务</div>';const percent=run.total?Math.round(run.completed/run.total*100):100,scope=run.instrument_ids?.length?'指定 '+run.instrument_ids.length+' 个标的':run.instrument_types.map(kindName).join('、')+'全量';return '<div class="run-head"><div><span class="badge">'+esc(triggers[run.trigger]||'手动')+'</span> <b>'+esc(names[run.status]||run.status)+'</b><div class="meta">'+esc(scope)+' · '+esc(run.interval)+' · '+esc(run.start)+' → '+esc(run.end)+'</div></div><div class="actions"><span class="mono">写入 '+run.bars_written.toLocaleString()+'</span><button class="danger small" id="cancel-sync">取消</button></div></div><div class="progress-track"><div class="progress" style="width:'+percent+'%"></div></div><div class="progress-line"><span>'+run.completed+' / '+run.total+' 个标的 · 成功 '+run.succeeded+' · 失败 '+run.failed+'</span><span>'+percent+'%</span></div>'}
async function refresh(){try{const body=await request('/v1/data-sync'),data=body.data;state.activeRun=data.active_run;byId('active-run').innerHTML=runCard(state.activeRun);byId('sync-all').disabled=!!state.activeRun||state.startSubmitting;updateSelection();renderSchedules(data.schedules)}catch(error){byId('error').textContent=error.message}}
async function startSync(instrumentIds,label){if(state.activeRun||state.startSubmitting)return;if(!confirm('确定同步'+label+'吗？'))return;state.startSubmitting=true;byId('error').textContent='';updateSelection();byId('sync-all').disabled=true;try{await request('/v1/data-sync/runs',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({instrument_types:[state.kind],...(instrumentIds?{instrument_ids:instrumentIds}:{}),interval:byId('interval').value,start:byId('start').value,end:byId('end').value,adjustment:'none',delay_ms:750})});await Promise.all([refresh(),loadRuns()])}catch(error){byId('error').textContent=error.message}finally{state.startSubmitting=false;await refresh()}}
function runStatus(run){return recoveryNames[run.recovery_status]||names[run.status]||run.status}
async function loadRuns(){try{const body=await request('/v1/data-sync/runs?limit=50');byId('run-list').innerHTML=body.data.length?body.data.map((run)=>'<div class="history-row"><div><span class="badge '+(run.remaining_failed>0?'error':'')+'">'+esc(runStatus(run))+'</span></div><div><div class="identity">'+esc(triggers[run.trigger]||'手动')+' · '+(run.interval==='1m'?'1 分钟':'日线')+'</div><div class="meta">'+time(run.started_at)+' · '+esc(run.instrument_ids?.length?'指定 '+run.instrument_ids.length+' 个标的':run.instrument_types.map(kindName).join('、')+'全量')+'</div>'+(run.retry_count?'<div class="meta">已重试 '+run.retry_count+' 次 · 仍失败 '+run.remaining_failed+' 个</div>':'')+'</div><div class="mono">成功 '+run.succeeded+' / 失败 '+run.failed+'</div><div class="mono">写入 '+run.bars_written.toLocaleString()+' 条</div><div class="actions"><button class="secondary small" data-detail="'+run.run_id+'">明细</button>'+(run.remaining_failed>0?'<button class="danger small" data-retry="'+run.run_id+'">重试失败项</button>':'')+'</div><div class="details" id="detail-'+run.run_id+'"></div></div>').join(''):'<div class="empty">尚无运行记录</div>'}catch(error){byId('run-list').innerHTML='<div class="empty">'+esc(error.message)+'</div>'}}
async function loadItems(runId){const box=byId('detail-'+runId);if(box.dataset.loaded){box.innerHTML='';delete box.dataset.loaded;return}box.innerHTML='<div class="empty">读取中…</div>';try{const body=await request('/v1/data-sync/runs/'+runId+'/items');box.dataset.loaded='1';box.innerHTML=body.data.length?body.data.map((item)=>'<div class="failure"><div>'+esc(item.name)+' · '+esc(item.symbol)+'<div class="meta">'+esc(item.instrument_id)+'</div></div><div>'+esc(item.error||names[item.status]||item.status)+'</div>'+(item.status==='failed'?'<button class="danger small" data-retry="'+runId+'" data-instrument="'+esc(item.instrument_id)+'">重试此标的</button>':'<span></span>')+'</div>').join(''):'<div class="empty">旧记录没有标的级明细</div>'}catch(error){box.innerHTML='<div class="empty">'+esc(error.message)+'</div>'}}
async function retry(runId,instrumentId){try{await request('/v1/data-sync/runs/'+runId+'/retry',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(instrumentId?{instrument_id:instrumentId}:{})});await Promise.all([refresh(),loadRuns()])}catch(error){byId('error').textContent=error.message}}
function renderSchedules(rows){byId('schedule-count').textContent=rows.length+' 个任务';byId('schedule-list').innerHTML=rows.length?'<table><thead><tr><th>类型与范围</th><th>周期</th><th>执行规则</th><th>下次执行</th><th>最近触发</th><th></th></tr></thead><tbody>'+rows.map((item)=>'<tr><td><div class="identity">'+item.instrument_types.map(kindName).join('、')+' · '+(item.instrument_ids?.length?'指定 '+item.instrument_ids.length+' 个':'全部标的')+'</div><div class="meta">任务 #'+item.schedule_id+'</div></td><td>'+(item.interval==='1m'?'1 分钟':'日线')+'</td><td>'+(item.skip_weekends?'周一至周五':'每天')+' · '+esc(item.time)+'</td><td>'+time(item.next_run_at)+'</td><td>'+time(item.last_triggered_at)+'</td><td class="schedule-actions"><button class="danger small" data-schedule-id="'+item.schedule_id+'">删除</button></td></tr>').join('')+'</tbody></table>':'<div class="empty">暂无定时任务</div>'}
function updateIntervalOptions(){const future=state.kind==='future';for(const select of [byId('interval'),byId('schedule-interval')]){const minute=select.querySelector('option[value="1m"]');minute.disabled=future;if(future)select.value='1d'}}
document.querySelectorAll('[data-kind]').forEach((button)=>button.onclick=()=>{state.kind=button.dataset.kind;state.offset=0;history.replaceState(null,'','?type='+state.kind);document.querySelectorAll('[data-kind]').forEach((item)=>item.classList.toggle('active',item===button));byId('kind-title').textContent=kindName(state.kind);byId('query').value='';updateIntervalOptions();if(state.kind==='crypto')byId('schedule-days').value='daily';loadInstruments();updateSelection()});document.querySelectorAll('[data-kind]').forEach((button)=>button.classList.toggle('active',button.dataset.kind===state.kind));byId('kind-title').textContent=kindName(state.kind);updateIntervalOptions();if(state.kind==='crypto')byId('schedule-days').value='daily';
document.querySelectorAll('[data-tab]').forEach((button)=>button.onclick=()=>{document.querySelectorAll('.tab,.tab-panel').forEach((item)=>item.classList.remove('active'));button.classList.add('active');byId(button.dataset.tab).classList.add('active')});byId('query').oninput=()=>{clearTimeout(state.searchTimer);state.searchTimer=setTimeout(()=>{state.offset=0;loadInstruments()},250)};byId('previous').onclick=()=>{state.offset=Math.max(0,state.offset-state.limit);loadInstruments()};byId('next').onclick=()=>{state.offset+=state.limit;loadInstruments()};
byId('refresh-catalog').onclick=async()=>{const button=byId('refresh-catalog');button.disabled=true;button.textContent='正在刷新…';byId('error').textContent='';try{const body=await request('/v1/data-sync/instruments/refresh',{method:'POST'});byId('catalog-refreshed-at').textContent=body.data.refreshed_at?'目录更新于 '+time(body.data.refreshed_at):'刷新未获得可用目录';await loadInstruments()}catch(error){byId('error').textContent=error.message}finally{button.disabled=false;button.textContent='刷新标的目录'}};
byId('instrument-list').onclick=(event)=>{const button=event.target.closest('[data-sync-one]');if(button)startSync([button.dataset.syncOne],button.dataset.name)};byId('instrument-list').onchange=(event)=>{const box=event.target.closest('[data-select]');if(box){box.checked?selected().add(box.dataset.select):selected().delete(box.dataset.select);updateSelection();return}if(event.target.id==='select-page'){state.items.forEach((item)=>event.target.checked?selected().add(item.instrument_id):selected().delete(item.instrument_id));renderInstruments()}};byId('sync-selected').onclick=()=>startSync([...selected()],'所选 '+selected().size+' 个'+kindName(state.kind));byId('sync-all').onclick=()=>startSync(null,'全部'+kindName(state.kind));byId('active-run').onclick=async(event)=>{if(event.target.id==='cancel-sync'){await request('/v1/data-sync/cancel',{method:'POST'});await refresh()}};
byId('refresh-runs').onclick=loadRuns;byId('clear-runs').onclick=async()=>{if(!confirm('确定清空全部运行记录吗？历史行情数据不会被删除。'))return;try{await request('/v1/data-sync/runs',{method:'DELETE'});await Promise.all([refresh(),loadRuns()])}catch(error){byId('error').textContent=error.message}};byId('run-list').onclick=(event)=>{const detail=event.target.closest('[data-detail]'),retryButton=event.target.closest('[data-retry]');if(detail)loadItems(detail.dataset.detail);if(retryButton)retry(retryButton.dataset.retry,retryButton.dataset.instrument)};
byId('save-schedule').onclick=async()=>{const button=byId('save-schedule'),useSelected=byId('schedule-scope').value==='selected',ids=useSelected?[...selected()]:null;button.disabled=true;byId('schedule-error').textContent='';try{await request('/v1/data-sync/schedules',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({interval:byId('schedule-interval').value,time:byId('schedule-time').value,skip_weekends:byId('schedule-days').value==='weekdays',instrument_types:[state.kind],...(ids?{instrument_ids:ids}:{}),lookback_days:Number(byId('schedule-lookback').value),adjustment:'none',delay_ms:750})});await refresh()}catch(error){byId('schedule-error').textContent=error.message}finally{button.disabled=false}};byId('schedule-list').onclick=async(event)=>{const button=event.target.closest('[data-schedule-id]');if(!button||!confirm('确定删除这个定时任务吗？'))return;await request('/v1/data-sync/schedules/'+button.dataset.scheduleId,{method:'DELETE'});await refresh()};
loadInstruments();refresh();loadRuns();setInterval(()=>{refresh();if(state.activeRun)loadRuns()},2500);
</script></body></html>`
