# 粗粒度退出码配合稳定 JSON 错误代码

CLI 以退出码 `0` 表示成功（包括空数据），`1` 表示已处理的运行时失败，`2` 表示命令或参数使用错误，`130` 表示调用方中断；网络、超时、鉴权、结果过大、序列化和文件错误不再分配独立数字。所有可处理失败（包括 Click 参数错误）在 stderr 输出最小 JSON，并以稳定的 `code`、可行动的 `message` 和 `retryable` 区分具体错误，使调用模型不必维护一套细碎的数字映射。
