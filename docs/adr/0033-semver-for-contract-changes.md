# 使用 EVA CLI 自己的语义化版本表达契约变化

EVA CLI 使用独立于 AKShare 的语义化版本，并由 `eva version` 同时报告两者。删除命令、改变路径语义、增加必填参数、删除或重命名字段以及改变字段含义或类型属于 major；新增命令、可选参数、返回字段或等价数据提供方属于 minor；不改变契约的修复和文档调整属于 patch。升级 AKShare 时必须先生成契约差异报告，由维护者确认删除、映射和相应版本级别后才能发布。
