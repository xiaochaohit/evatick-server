# 股票日线在主数据源暂时失败时降级

稳定命令 `eva stock bars` 首选 AKShare 的东方财富日线接口；当且仅当日频调用发生连接失败、超时或可重试 HTTP 错误时，自动将股票代码和日期转换为新浪接口格式，并调用 AKShare 的新浪日线接口。周线和月线不做不等价降级，确定性参数及解析错误也不得被掩盖。每条稳定命令结果记录通过 `_eva_source` 标明实际使用的 `eastmoney` 或 `sina`；对应的 AKShare 上游命令保持原样，不获得隐式降级行为。
