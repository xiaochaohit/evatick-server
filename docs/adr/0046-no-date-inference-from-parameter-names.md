# 日期输入只按真实类型转换，不按名称猜测

注解为 `date`、`datetime` 或相应可选类型的参数接受 ISO 8601 并转换为 Python 对象；实际类型为字符串时，即使名称包含 date、time 或 year 也原样传递，只按静态资料明确记载的格式校验，不自动转换 `YYYYMMDD` 与 `YYYY-MM-DD`。签名和日期文档冲突时报告警告，人工覆盖可补充校验；所有日期时间输出仍统一为 ISO 8601。
