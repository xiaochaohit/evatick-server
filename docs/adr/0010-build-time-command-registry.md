# 构建时生成静态命令注册表

CLI 针对绑定的 AKShare 版本，在构建时扫描根命名空间、函数签名、类型注解、docstring 和官方文档，生成随包发布的静态命令注册表。运行时只加载领域索引，并在实际调用命令时惰性导入目标函数，不在每次启动时重新扫描 AKShare。

## Consequences

注册表记录命令路径、上游函数、模块、参数、返回类型、稳定性、说明和来源。CI 必须重新生成注册表并检查无未提交差异，同时比较公共数据函数集合以阻止遗漏；升级 AKShare 需要重新生成、验证并发布 CLI。静态注册表是帮助与运行时绑定版本保持一致的依据。
