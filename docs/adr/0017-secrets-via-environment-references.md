# 敏感参数只能引用环境变量

绑定版本中至少有二十个公共函数接收 token、cookie 或 API key。敏感值若直接出现在命令参数，会暴露于进程列表和命令历史，因此生成器只为 `token`、`cookie`、`api_key`、`password` 和 `secret` 等参数提供相应的 `--*-env` 环境变量引用，不提供明文选项，也禁止通过 `--args-json` 传入。

## Consequences

帮助必须说明所需环境变量但不得显示值；错误、日志和诊断必须脱敏。缓存键可以包含敏感值的不可逆摘要以区分身份，但缓存元数据和持久化内容不得保存原始凭据。
