# 为上游命令生成最小且安全的示例

官方资料中的 Python 示例可能过期、包含不适合 shell 的值或暴露敏感参数，不能可靠地批量转换成 CLI 示例。稳定语义命令使用人工维护并测试的可执行示例；自动生成的上游命令只生成包含必填选项的单个最小示例，普通值使用明显占位符，敏感值只展示环境变量引用，无必填项时只展示命令路径；`registry_overrides.yaml` 可用经过测试的真实 CLI 示例覆盖机械结果。
