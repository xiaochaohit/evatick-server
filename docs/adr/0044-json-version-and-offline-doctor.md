# 版本与诊断命令提供稳定 JSON，诊断默认离线

`market-cli version` 返回 Market CLI、Python、各数据提供方版本和注册表 SHA-256；`market-cli doctor` 返回总体状态与逐项检查，默认验证注册表、绑定版本、提供方导入、工作进程、缓存目录和可选 Parquet 支持而不联网。只有显式 `--network` 才运行预先指定的轻量连通检查；警告退出 `0`，必需能力失败退出 `1`，任何输出都不得包含秘密值、请求头、用户主目录等非必要环境信息。
