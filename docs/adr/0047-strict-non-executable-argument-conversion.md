# 参数转换严格且不可执行

标量使用与区域无关的标准格式，Literal 与枚举限制 choices，布尔值使用正反开关，列表使用重复选项，字典、嵌套及无法安全表达的复杂值只接受 `--args-json` JSON 对象。`--args-json` 可与框架选项混用但不能与任何单独数据参数混用，未知键、类型错误和非法 null 在调用前失败，敏感参数仍只能引用环境变量；禁止 Python literal、`eval`、对象导入或任何可执行表达式。
