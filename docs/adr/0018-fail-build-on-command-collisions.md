# 命令路径冲突必须显式解决

公共函数通过确定性规则映射为领域命令，稳定语义命令拥有保留名称。生成器发现两个不同上游函数映射到同一路径，或自动生成命令与稳定命令重名时，必须使构建失败，并要求在版本控制的 `registry_overrides.yaml` 中显式指定新路径；不得自动追加数字或不透明后缀。

同一公共函数被重复导入时只去重，不视为冲突。当前绑定版本纳入的 1,090 个公共数据函数可以生成 1,090 个唯一命令路径，没有不同函数间的冲突。
