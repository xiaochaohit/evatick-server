# 从上游函数确定性生成并冻结命令路径

生成器先通过版本控制中的映射表确定领域，再仅移除函数名开头与领域重复的前缀，并将剩余名称机械地从小写下划线转换为连字符；市场、国家、品种和数据源等限定词必须保留，不能自动翻译、缩写或语义美化。首次发布前可用 `registry_overrides.yaml` 修正路径和解决冲突；发布后以注册表中的路径为权威，生成规则改变不得自动重命名已有命令，只有 ADR 0032 定义的升级人工决策可以删除或重新映射路径。
