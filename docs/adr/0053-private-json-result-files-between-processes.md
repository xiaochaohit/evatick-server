# 工作进程通过私有 JSON 文件传递结果

每次数据调用使用独立进程组中的全新 spawn 工作进程，总预算覆盖启动、导入、调用、重试和序列化；受限管道只返回状态、记录数、临时路径、字节数和 SHA-256，大结果写入权限受限的版本化规范 JSON 临时文件，禁止 pickle。监督进程验证退出状态与校验值后才输出、缓存或导出，并捕获提供方意外输出；超时或中断终止整个进程组、清理临时文件，后续启动只清理带有效标记且超过安全阈值的遗留文件。
