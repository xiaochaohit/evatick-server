# Python 函数签名是参数契约的唯一权威来源

AKShare 的函数签名、docstring 和官方 Markdown 可能不同步；若让说明文档覆盖实际签名，生成器可能暴露无法调用的参数。注册表因此只以实际 Python 函数签名确定参数名、必填性、默认值和类型，docstring 与官方 Markdown 仅用于补充说明和示例：文档缺失时报告缺口而不猜测，文档出现签名中不存在的参数时警告而不暴露，只有无法可靠读取或调用签名时才使构建失败。
