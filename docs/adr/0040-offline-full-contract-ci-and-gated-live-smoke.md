# 离线 CI 覆盖全部契约，发布门禁运行代表性实时冒烟

每次提交的 CI 禁止联网，并必须验证纳入函数全量覆盖、排除理由、路径唯一与差异、全部生成命令的加载帮助参数转换和模拟调用，以及序列化、输出预算、文件、缓存、重试、进程超时、秘密参数和错误脱敏；全文帮助快照只覆盖代表性命令。升级或发布时为每个稳定领域和 `alternative` 子领域运行至少一个无需秘密凭据的实时冒烟，受凭据或地域限制的跳过项必须说明理由；实时失败默认阻止发布，只有维护者明确批准并在发布报告留痕时才能豁免外部数据源故障。
