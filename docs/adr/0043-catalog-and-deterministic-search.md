# 使用领域目录和确定性搜索发现命令

`market-cli catalog` 以 JSON 返回领域说明、命令总数及稳定性计数而不展开全部命令；`market-cli search` 要求至少提供 `--query` 或 `--domain`，可按稳定性过滤，默认返回 20 条且最多 200 条。搜索覆盖路径、用途和原函数名，并按完整路径、路径前缀、词元和普通子串确定性排序；结果只含路径、用途、稳定性、提供方和原函数名，完整参数与示例继续通过叶子命令 `--help` 获取。
