# 临时网络错误默认重试两次

AKShare 依赖多个公开网站，短暂断连、限流和网关错误较常见，而参数与解析错误重复调用不会自行恢复。CLI 对连接中断、连接超时、HTTP 429、502、503 和 504 默认额外重试两次并采用短指数退避；其他错误立即失败，调用者可用 `--retries 0` 关闭重试。

## Consequences

错误分类必须区分临时网络失败和确定性失败。最终错误在标准错误中以最小 JSON 输出错误代码、可行动消息和 `retryable`；重试次数是框架级公共选项，不传递给上游函数。
