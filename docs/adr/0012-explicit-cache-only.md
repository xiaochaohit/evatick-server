# 缓存必须由调用者显式启用

实时行情、历史行情和宏观数据对新鲜度的要求不同，而全量自动生成命令无法可靠推断每个数据集的安全缓存周期。CLI 因此默认不缓存并保持与直接调用 AKShare 一致；模型只有通过 `--cache-ttl` 才启用结果缓存，并可用 `--refresh` 绕过已有缓存。

## Consequences

缓存键必须包含命令路径、完整调用参数和绑定的 AKShare 版本。缓存是框架级公共选项，不传递给上游函数；缓存管理命令可以检查和清理缓存，但不得引入隐式领域 TTL。
