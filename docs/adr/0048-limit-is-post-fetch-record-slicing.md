# `--limit` 是获取后的记录切片

正整数 `--limit N` 在数据提供方完整返回后按原始顺序保留前 N 条，不减少上游网络、计算或工作进程内存；提供方自身的同名参数通过 `--arg-limit` 独立控制。该选项只适用于 DataFrame、Series、数组和记录列表，其他结果返回 `LIMIT_NOT_APPLICABLE`；stdout 的 200 行预算在切片后检查，文件导出同样只写入切片结果。
