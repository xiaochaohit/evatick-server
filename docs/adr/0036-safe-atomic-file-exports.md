# 导出和诊断文件使用安全的原子写入

`--output PATH` 与 `--debug-output PATH` 默认拒绝覆盖已有路径，只有显式 `--overwrite` 才能替换普通文件，并始终拒绝目录、符号链接和设备文件。写入必须在目标目录内先完成临时文件、刷新成功后再原子替换，失败或中断时清理临时文件；CLI 不自动创建父目录，相对路径以当前工作目录为基准，成功导出返回规范化绝对路径和记录数。
