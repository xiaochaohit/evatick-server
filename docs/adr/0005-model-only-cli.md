# CLI 只面向大模型调用

CLI 的唯一目标调用者是大模型智能体，因此命令发现、参数描述、结果和错误优先采用稳定的机器可读契约，不为人工终端体验设计彩色表格、交互式提示或冗长叙述性帮助。这缩小了产品边界，也避免同时维护人类界面和模型界面造成歧义。

## Consequences

帮助必须包含命令路径、用途、参数、稳定性、数据来源、返回说明和示例；命令不能依赖交互式输入。面向人工阅读的展示只保留为调试便利，不得影响模型契约。
