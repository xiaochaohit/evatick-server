# 缓存按用户隔离并保存规范 JSON

只有正整数 `--cache-ttl` 才启用结果缓存，`--refresh` 绕过旧值并在成功后更新；缓存位于操作系统标准的每用户目录，目录和文件仅当前用户可访问。键包含路径、提供方及版本、完整数据参数和影响结果的框架参数，秘密只参与不可逆摘要；只缓存成功结果的规范严格 JSON，错误和部分结果不缓存。过期项惰性删除，首版不后台淘汰；`market-cli cache status`、`cache clear --expired` 和显式 `cache clear --all` 返回条目及字节统计。
