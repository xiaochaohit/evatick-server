# 跨版本只固化已发布的命令路径

为完整提供 AKShare 能力且允许未来接入其他数据提供方，首版按 AKShare 公共函数设计生成全部领域命令，不按主观重要性筛选；生成器不得自行改名或删除已经正式发布的命令。兼容性承诺不延伸到参数和返回字段，它们可以随 CLI 及绑定数据提供方的明确版本升级而调整；升级必须作为可见的版本变化交付，不能在运行时静默改变。ADR 0032 后来允许人在升级审核中明确决定删除或重新映射路径。该决定取代 ADR 0024 对输入语义和输出结构长期不变的要求。
