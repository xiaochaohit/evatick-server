# 数据提供方升级必须经人工审核并可由 CI 复现

升级工具在隔离环境比较当前与目标 AKShare，报告函数增删和改名候选、路径冲突、参数及已知返回变化、文档缺口和排除项变化，并对删除、映射、冲突及无法分类项逐一询问维护者。决定必须写入 `registry_overrides.yaml` 和版本化升级报告，再生成注册表、帮助及夹具，运行离线 CI 与实时冒烟，由维护者确认 SemVer、豁免和发布；CI 只验证非交互复现，只有显式版本标签发布操作才能构建和上传包。
