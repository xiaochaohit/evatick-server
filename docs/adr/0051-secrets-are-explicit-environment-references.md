# 秘密使用受校验的环境变量引用

敏感选项只接受匹配 `[A-Za-z_][A-Za-z0-9_]*` 的环境变量名称，不解析 shell 表达式；未设置和空值分别返回 `MISSING_SECRET` 与 `EMPTY_SECRET`，非空值原样传递。秘密不得进入子进程命令行、错误、诊断、注册表、帮助、缓存元数据或发布报告，缓存身份使用每用户随机密钥的 HMAC；首版不读取 `.env`、系统钥匙串或明文秘密配置。
